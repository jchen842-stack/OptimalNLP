__pycache__/
*.pyc
.DS_Store
*.log
# large data / caches never belong in this repo (they live on the PVC)
datasets/
data/
*.npz
*.pkl
models/*
!models/README.md
*.pth
*.pt
# corpora and checkpoints live outside the repo (see REPRODUCE.md)
*.feats
snli_1.0*/
*.jsonl

# clean upstream clone provisioned by verify/run_all.sh for the patch no-op check
.upstream-clean/
# rendered diary output; .md is the source (scripts/make_diary_docx.sh regenerates)
*.docx
